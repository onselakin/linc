import { XTermSection } from '../../model/scenario';

interface MarkdownProps {
  configuration: XTermSection;
}

const Markdown = ({ configuration }: MarkdownProps) => {
  return <div />;
};
export default Markdown;
